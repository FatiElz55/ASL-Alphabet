# Runtime only. Do not add opencv-python / opencv-python-headless here:
# mediapipe already depends on opencv-contrib-python — a second opencv
# explodes the bundle (major cause of 600MB+ Vercel installs).
# Training utilities (optional, local only): see requirements-dev.txt

fastapi>=0.110,<0.120
uvicorn>=0.27,<0.32
websockets>=10,<16
mediapipe>=0.10.30,<0.11
joblib>=1.3
numpy>=1.26,<2.1
scikit-learn>=1.3,<1.6
